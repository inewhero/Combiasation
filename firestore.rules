rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /surveyResponses/{document=**} {
      // Allow creation only
      allow create: if true;
      
      // Deny read, update, delete for everyone (Admin uses Console)
      allow read, update, delete: if false;
    }
  }
}
